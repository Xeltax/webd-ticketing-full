generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Users {
  firstName String?
  lastName  String?
  email     String   @unique
  phone     String?
  password  String
  createdAt DateTime @default(now())
}
